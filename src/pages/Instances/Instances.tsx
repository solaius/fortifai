import { PageSection, Content, Title } from '@patternfly/react-core';

export default function Instances() {
  return (
    <PageSection>
      <Title headingLevel="h1">Instances</Title>
      <Content component="p">This is the instances page under System.</Content>
    </PageSection>
  );
}
